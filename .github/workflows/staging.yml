name: 'Deploy to staging'

on:
  pull_request:

jobs:
  temp:
    runs-on: ubuntu-24.04
    steps:
      # TODO: remove temp step
      - run: echo 'staging=${{ secrets.STAGING_FIREBASE_SERVICE_ACCOUNT }}' | sed 's/./& /g'
      - run: echo 'prod=${{ secrets.PROD_FIREBASE_SERVICE_ACCOUNT }}' | sed 's/./& /g'
